a=[1,2,4]
b=[1,3,4]
c=zip(a,b)
#m=0
#c = []
#for i, j in zip(a, b):

   # c+=[i, j]
    #c.append(m)
print(c)   
#o/p-[1,1,2,3,4,4]
#zip():The zip() function returns a zip object, which is an iterator of tuples where the first item in each passed iterator is paired together, and then the second item in each passed iterator are paired together etc.Iterator objects that will be joined together